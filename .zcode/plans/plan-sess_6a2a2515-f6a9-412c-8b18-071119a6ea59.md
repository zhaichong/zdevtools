我已经通过探针代理核查了当前的系统状态。先前的确切进度停留在了脱敏注入安全阶段，有关**旧有文件数据轮转**、**HDC Bug加固**以及**业务特判脱钩**尚未被执行。

现在我为您制定如下最终补充方案以彻底了结整个修复排期：

### 1. 挂载数据轮转清理，防范诊断快照与录屏吃满硬盘（对应：2.4 - 2.5）
- **修复目标:** 修改 `src/main/ipc/diagnostic.js` 和 `src/main/ipc/rrweb.js`，避免无止尽追加或新开记录文档。
- **实施:**
  - 分别在两个文件主方法内构建 `cleanupOldRuns`（清理非最近20次的运行时诊断三件套：`.json`,`.jsonl`,`.report.json`）与 `cleanupOldChunks`（保留最新 10 个录屏持久化分片）。
  - 在 `setupDiagnosticIpc` 和 `setupRrwebIpc` 初始化暴露时以 `catch` 包裹作为**静默非阻塞后台任务**执行一次（`fsp.readdir` 结合按 `mtime` 裁剪），解决磁盘爆满隐患。

### 2. 彻底抽离特供业务判断至 Configuration（对应：3）
- **修复目标:** 从 `useRootCauses.js` 等基础工具模块中拔除对 `ROLL_DEPT`、`MATTRESS`等具体独占业务链路的硬编码识别。
- **实施:** 
  - 扩张 `src/shared/constants/profiles.js`，为不同的项目端（如 `yarward-ntv-frontend`）挂载独立的 `globalMethodRules` (正则模式、owner分配、操作指南)、特定的 `triggerHints` 与 `storageKeys` 白名单。
  - 改造 `useRootCauses.js` 中的 `inferTrigger`, `explainGlobal` 等方法。将原有的硬编码 Regex 替换为通过 `profileId` 动态迭代 `getProfileConfig` 中下发的策略表。实现基础监控代码的**业务透明**能力。

### 3. HDC 工具集成 Bug 修复（对应：4）
- **修复目标:** 解决因三元和兜底符误用导致的命令格式失效，以及状态识别死门。
- **实施 (`hdcDriver.js`):**
  - **空设备标志:** 把 `['fport', 'rm', ...]` 操作对齐至原生 API，即当解析不到设备的 `f.id` 甚至其等于 `'*'` 时，直接摘除掉命令中的 `-t [id]` 片段，而非使其重新变回 `hdc -t * fport rm` 的错误命令。
  - **解析在线状态:** 对 `parseDevices` 内置真实判断，匹配 `unauthorized` 与 `offline` 字符作为设备状态返回，而非盲目将所有行打上 `'device'` 的标记位。
  - **可用度反馈:** 在 `checkAvailability` 内，如果失败判定由于找不到指令集或报错类似 `ENOENT`，增加直接明了的文本指导："未在 bin/ 目录下或 PATH 环境变量找到 hdc.exe，请查证。"。

---

如果这个实施方案没有问题，您授权后我将立刻动用脚本开始上述代码的最终编辑工作。