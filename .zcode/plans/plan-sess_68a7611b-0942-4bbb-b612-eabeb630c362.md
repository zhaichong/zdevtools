## 剩余技术债修复计划

基于逐行审计，共 7 项可修复问题。按优先级分为 3 批：

### 第一批：代码卫生（4 项，小工作量，无风险）

1. **删除死代码 `isDev` + 内联 electron require** — `src/server/index.js:18`，赋值后从未使用
2. **删除 `redactSensitive` 死别名** — `src/shared/utils/redact.js:21`，全仓库 0 处引用
3. **修复探针 `||` vs `??` 与 redact.js 不一致** — `useProbe.js:99`，统一为 `value ?? ''`
4. **优化 rrweb 导入路径** — `useProbe.js:2`，在 `vite.config.js` 加 resolve alias，useProbe 改为 `import rrwebScript from 'rrweb-umd?raw'`

### 第二批：可维护性重构（2 项，中等工作量）

5. **提取 `pauseAll()` / `resumeAll()`** — `useWorkbenchSession.js:193-229`，消除 14 次散落调用，3 个重复代码块统一为 2 个函数调用
6. **拆分 `collect()`** — `useWorkbenchSession.js:81-156`，76 行 8 phase 拆为 `reconnectCdp()` + `setupRrwebBindings()` + `captureSnapshot()` 3 个 helper

### 第三批：模板规范化（1 项，小工作量）

7. **统一 App.vue 模板 ref 访问约定** — `App.vue:173-206`，解构 `logcatManager`/`cdpClient`/`diagnosticRun` 的 ref，模板中统一去掉 `.value`

### 修改文件清单

| 文件 | 改动 |
|------|------|
| `src/server/index.js` | 删 1 行死代码 |
| `src/shared/utils/redact.js` | 删 2 行死别名 |
| `src/pages/workbench/composables/useProbe.js` | 改 import 路径 + 修 `||` → `??` |
| `vite.config.js` | 加 1 个 resolve alias |
| `src/pages/workbench/composables/useWorkbenchSession.js` | 提取 `pauseAll/resumeAll` + 拆分 `collect()` |
| `src/pages/workbench/App.vue` | 解构 logcatManager/cdpClient/diagnosticRun refs |

共 6 个文件，预计净减少约 20 行代码。